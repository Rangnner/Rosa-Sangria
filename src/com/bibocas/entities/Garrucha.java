package com.bibocas.entities;

import java.awt.image.BufferedImage;

public class Garrucha extends Entity {
    public Garrucha(int x, int y, int width, int height, BufferedImage spriteSheet) {
        super(x, y, width, height, spriteSheet);
    }
}
